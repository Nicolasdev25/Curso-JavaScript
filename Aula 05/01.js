//Lista de valores (array)
const numeros =[];

//atribuição para add elementos
numeros[0] = 1;
numeros[1] = 2;
numeros[2] = 3;

// acesso

console.log(numeros);//acessando geral
console.log(numeros[1]) //acessando idice de posição espefico

//manipulação
numeros[0] = 0;
console.log(numeros);

//inicialização
const notas = [7.6, 4.5, 8.7];
console.log(notas);